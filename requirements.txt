torch==2.2.1  # Use the latest available version (2.x series)
opencv-python==4.10.0
streamlit==1.41.1  # Streamlit version from logs (or keep the version you're using)
tensorflow==2.13.0
numpy==1.25.0
